# 算疏智合快速上手

## 1. 安装依赖

```bat
install_deps.bat
```

## 2. 启动 GUI

```bat
start_gui.bat
```

## 3. 配置模型

在 GUI 中填写 API Base、API Key 和模型名；也可以选择本地模型服务并填写本地服务地址。

## 4. 上传资料并生成

按照界面步骤上传公司资料、选择组织架构、录入组织成员资料，最后确认生成。

## 5. 查看结果

生成结果位于：

```text
companies/<company_slug>/
```

Agent 导出位于：

```text
agent_exports/<company_slug>/
```
